# 10-05

Qual é o problema conceitual com o trecho a seguirdo código Apache Spark, destinado a atuar sobre dados muito grandes? Observe que a função `collect()` retorna uma coleção Java, e as coleções Java (do Java 8 em diante) suportam as função de `map` e `reduce`.

```
JavaRDD<String< lines = sc.textFile("logDirectory");
int tamanhoTotal = lines.collect().map(s -> s.length())
                    .reduce(0,(a,b) -> a+b);
```

## Resposta
