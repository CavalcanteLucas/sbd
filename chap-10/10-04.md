# 10-04

Mostre o pseudocódigo para calcular uma junção $r \bowtie_{r\cdot A = s\cdot A}s$ usando uma única etapa MapReduce, supondo que a função `map()` seja chamada em cada tupla de $r$ e $s$. Suponha que a função `map()` possa encontrar o nome da relação usando `context.relname()`.

## Resposta
