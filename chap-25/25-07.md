# 25-07

Suponha que um programador de aplicação tivesse que escrever uma consulta

<pre>
    <b>select</b> *p.a1*
    <b>from</b> r <b>natural left outer join</b> s;
</pre>

sobre as relações $r$($A$,$B$) e $s$($A$,$B$), mas em vez disso tenha escrito

<pre>
    <b>select</b> *p.a1*
    <b>from</b> r <b>natural join</b> s;
</pre>

**a)** Crie dados de exemplo para $r$ e $s$ para os quais as duas consultas gerem o mesmo resultado.

**b)** Crie dados de exemplo para $r$ e $s$ para os quais duas consultas gerem resultados diferentes, expondo, assim, o erro na formulação da consulta.

## Resposta
