# 03-09

Considere o banco de dados relacional da Figura 3.19, onde as chaves primárias estão sublinhadas. Dê uma expressão SQL para cada uma das seguintes consultas.

<sub>**Figura 3.19** - Banco de dados de empregado.</sub>
<pre>
    empregado(<ins>ID</ins>, nome_empregado, rua, cidade)
    trabalha(<ins>ID</ins>, nome_empresa, salário)
    empresa(<ins>nome_empresa</ins>, cidade)
    gerencia(<ins>ID</ins>, id_gerente)
</pre>

**a)** Ache os nomes e cidades de residência de todos os empregados que trabalham para a "First Bank Corporation".

**b)** Ache as IDs, os nomes, ruas e cidades de residência de todos os funcionários que trabalham para a "First Bank Corporation".

**c)** Ache as IDs de todos os empregados no banco de dados que não trabalham para a "First Bank Corporation".

**d)** Ache as IDs de todos os empregados no banco de dados que ganham mais do que cada empregado da "Small Bank Corporation".

**e)** Suponha que as empresas possam estar localizadas em várias cidades. Ache todas as empresas localizadas em cada cidade em que a "Small Bank Corporationn" está localizada.

**f)** Ache o nome da empresa que tem mais empregados (ou empresas, caso haja um empate na quantidade de empregados).

**g)** Ache o nome de cada empresa cujos empregados ganham um salário mais alto, na média, do que o salário médio na "First Bank Corporation".

## Resposta

### a)

```sql 03-09-a.sql
select
    gado.nome_empregado,
    gado.cidade
from
    empregado.empregado gado,
    empregado.trabalha t
where
    gado.id = t.id
    and lower(t.nome_empresa) like '%first bank%';
```
| nome_empregado | cidade  |
| -------------- | ------- |
| Ruth           | Caruaru |
| Ash            | Recife  |
| Frodo          | Recife  |
| Bilbo          | Recife  |
| Sauron         | Recife  |
| Enzo           | Belo Jardim |

### b)

```sql 03-09-b1.sql
select
    gado.id,
    gado.nome_empregado,
    gado.rua,
    gado.cidade
from
    empregado.empregado gado
where
    id in (
        select
            id
        from
            empregado.trabalha t
        where
            lower(t.nome_empresa) like '%first bank%'
            and t.salario > 10000
    );
```
| id    | nome_empregado | rua          | cidade  |
| ----- | -------------- | ------------ | ------- |
| 39991 | Frodo          | Rua Suja     | Recife  |
| 39993 | Sauron         | Rua Perigosa | Recife  |
| 12345 | Enzo           | Rua Direita  | Belo Jardim |

```sql 03-09-b2.sql
select
    gado.id,
    gado.nome_empregado,
    gado.rua,
    gado.cidade
from
    empregado.empregado gado
where
    exists (
        select
            1
        from
            empregado.trabalha t
        where
            gado.id = t.id
            and lower(t.nome_empresa) like '%first bank%'
            and t.salario > 10000
    );
```
| id    | nome_empregado | rua          | cidade  |
| ----- | -------------- | ------------ | ------- |
| 39991 | Frodo          | Rua Suja     | Recife  |
| 39993 | Sauron         | Rua Perigosa | Recife  |
| 12345 | Enzo           | Rua Direita  | Belo Jardim |

### c)

```sql 03-09-c.sql
select
    t1.id
from
    empregado.trabalha t1
where
    t1.id not in (
        select
            t2.id
        from
            empregado.trabalha t2
        where
            lower(t2.nome_empresa) like ('%first bank%')
    );
```
| id    |
| ----- |
| 12337 |
| 12339 |
| 12341 |
| 12338 |
| 39995 |
| 39996 |
| 12340 |
| 12342 |
| 20001 |
| 20002 |
| 20003 |
| 12344 |
| 39994 |

### d)

```sql 03-09-d1.sql
select
    t.id
from
    empregado.trabalha t
where
    t.salario > (
        select
            max(t.salario)
        from
            empregado.trabalha t
        where
            lower(t.nome_empresa) like '%small bank%'
    );
```
| id    |
| ----- |
| 39991 |
| 39993 |
| 12337 |
| 12342 |

```sql 03-09-d2.sql
select
    t.id
from
    empregado.trabalha t
where
    t.salario > all(
        select
            t.salario
        from
            empregado.trabalha t
        where
            lower(t.nome_empresa) like '%small bank%'
    );
```
| id    |
| ----- |
| 39991 |
| 39993 |
| 12337 |
| 12342 |

### e)

```sql 03-09-e.sql
select distinct
    nome_empresa
from
    empregado.empresa sa
where
    sa.cidade in (
        select
            sa.cidade
        from
            empregado.empresa sa
        where
            lower(sa.nome_empresa) like '%small bank%'
    )
    and lower(sa.nome_empresa) not like '%small bank%';
```
| nome_empresa           |
| ---------------------- |
| Moura                  |
| Feira                  |
| First Bank Corporation |
| Labcodes               |

### f)

```sql 03-09-f.sql
select
    t.nome_empresa
from
    empregado.trabalha t
group by
    nome_empresa
having
    count(t.id) >= all (
        select
            count(t.id) as res
        from
            empregado.trabalha t
        group by
            nome_empresa
    );
```
| nome_empresa           |
| ---------------------- |
| First Bank Corporation |

### g)

```sql 03-09-g.sql
select
    nome_empresa
from
    empregado.trabalha t
group by
    t.nome_empresa
having
    avg(salario) > (
        select
            avg(salario)
        from
            empregado.trabalha t
        where
            lower(t.nome_empresa) like '%first bank%'
    );
```
| nome_empresa           |
| ---------------------- |
| Feira                  |
| Small Bank Corporation |
| Labcodes               |