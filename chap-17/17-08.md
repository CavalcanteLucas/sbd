# 17-08

Considera-se que a anomalia da **atualização perdida** ocorre se uma transação $T_j$ lê um item de dados, depois outra transação $T_k$ grava o item de dados (possivelmente, com base em uma leitura anterior) e, então, $T_j$ grava o item de dados. A atualização realizada por $T_k$ foi perdida, pois a atualização feita por $T_j$ ignorou o valor gravado por $T_k$.

**a)** Dê um exemplo de *schedule* mostrando a anomalia da atualização perdida.

**b)** Dê um exemplo de *schedule* para mostrar que a anomalia da atualização perdida é possível com o nível de isolamento de **leitura confirmada**.

**c)** Explique por que a anomalia da atualização perdida não é possível com o nível de isolamento de **leitura reprodutível**.

## Resposta
