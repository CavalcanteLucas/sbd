# 07-09

Dado o esuema de banco de dados $R(A, B, C)$ e a relação $r$ no esquema $R$, escreva uma consulta SQL para testar se a dependência funcional $B \rightarrow C$ se aplica na relação $r$. Escreva também uma assertiva SQL que imponha a dependência funcional. Considere que nenhum valor nulo está presente. (Embora sendo parte do padrão SQL, essas assertivas não são aceitas por todas as implementações de banco de dados atualmente.)

## Resposta
