# 21-06

Os sistemas de banco de dados paralelos armazenam réplicas de cada item de dados (ou partição) em mais de um nó.

**a)** Por que é uma boa ideia distribuir as cópias dos itens de dados alocados a um nó por vários outros nós, em vez de armazenar todas as cópias no mesmo nó (ou conjunto de nós)?

**b)** Quais são as vantagens e as desvantagens de usar o armazenamento RAID em vez de armazenar uma cópia extra de cada item de dados?

## Resposta
