# 23-13

Suponha que você tenha uma relação grande $r(A,B,C)$ e uma *view* materializada $v=_A\gamma_{sum(B)}(r)$. A manutenção de *view* pode ser realizada como parte de cada transação que atualiza $r$, em um sistema de armazenamento paralelo/distribuído que admita transações por múltiplos nós. Suponha que o sistema use o *commit* de duas fases junto com um protocolo de consenso como Paxos, por *datacenters* espalhados geograficamente.

**a)** Explique por que não é uma boa idéia realizar a manutenção de *view* como parte da transação de atualização, se alguns valores do atributo $A$ são "quentes" em certos momentos, ou seja, muitas atualizações são feitas sobre esses valores de $A$.

**b)** Explique como o bloqueio de operação (se for aceito) poderia resolver esse problema.

**c)** Explique as vantagens e desvantagens de usar a manutenção de *view* assíncrona neste contexto.

## Resposta
