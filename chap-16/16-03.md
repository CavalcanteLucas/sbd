# 16-03

Para cada um dos seguintes pares de expressões, indique exemplos de relações que mostram que as expressões não são equivalentes.

**a)** $\prod_A(r-s)$ e $\prod_A(r) - \prod_A(s)$.

**b)** $\sigma_{B<4}({_A}\gamma_{max(B)\ as\ B}(r))$ e ${_A}\gamma_{max(B)\ as\ B}(\sigma_{B<4}(r))$.

**c)** Nas exprssões anteriores, se as duas ocorrências de *max* forem substituídas por *min*, as expressões serão equivalentes?

**d)** $(r\ ⟖\ s)\ ⟖\ t$ e $r\ ⟖\ (s\ ⟖\ t)$.

**e)** $\sigma_{\theta}(E_1\ ⟕\ E_2)$ e $E_1\ ⟕\ \sigma_\theta(E_2)$, em que $\theta$ usa apenas atributos de $E_2$.

## Resposta
