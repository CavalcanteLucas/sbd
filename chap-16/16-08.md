# 16-08

Considere a consulta:

<pre>
    <b>select</b> *
    <b>from</b> r, s
    <b>where</b> upper(r⋅A) = upper(s⋅A)
</pre>

em que "upper" é uma função que retorna seu argumento de entrada com todas as letras minúsculas substituídas pelas letras maiúsculas correspondentes.

**a)** Descubra que plano é gerado para essa consulta no sistema de banco de dados que você utiliza.

**b)** Alguns sistemas de banco de dados usariam uma junção de *loop* aninhado (em bloco) para essa consulta, o que pode ser muito ineficaz. Explique resumidamente como a junção *hash* ou a junção *merge* pode ser usada para essa consulta.

## Resposta
