# 16-09

Indique condições sob as quais as seguintes expressões são equivalentes:

${_{A,B}}\gamma_{agg(C)}(E_1\ \bowtie\ E_2)$ e $(_{A}\gamma_{agg(C)}(E_1))\ \bowtie\ E_2$

em que *agg* indica qualquer operação de agregação. Como essas condições podem ser relaxadas se *agg* puder ser restrito a **min** ou **max**?

## Resposta
