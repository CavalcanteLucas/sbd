# 14-06

Dê o pseudocódigo de uma função `findRangeIterator()` da árvore $B^+$, semelhante à função `findRange()`, exceto pelo fato de que ela retorna um objeto de iteração, como descrito na Seção 14.3.2. Dê também um pseudocódigo para a classe de iteração, incluindo as variáveis no objeto de iteração e o método `next()`.

## Resposta
