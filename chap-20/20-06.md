# 20-06

Considere um par de processos, em um sistema de memória compartilhada, tal que o processo $A$ atualiza uma estrutura de dados, e depois marca uma $flag$ para indicar que a atualização foi concluída. O processo $B$ monitora a $flag$ e inicia o processamento da estrutura de dados somente depois que descobre que a $flag$ está marcada.

Explique os problemas que poderiam surgir em uma arquitetura de memória onde as escritas podem ser reordenadas, e explique como as instruções `sfence` e `lfence` podem ser usadas para garantir que o problema não ocorra.

## Resposta
