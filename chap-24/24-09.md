# 24-09

Sugira um modo eficiente de testar se atabela de endereços de *bucket* no *hashing* extensível pude ser reduzida em tamanho, armazenando um contador extra com a tabela de endereços de *bucket*. Dê os detalhes de como a contagem deve ser mantida quando os *buckets* são divididos, unidos ou excluídos. (Nota: a redução do tamanho da tabela de endereços de *bucket* é uma operação dispendiosa, e as inserções seguintes podem fazer com que a tabela cresça novamente. Portanto, é melhor não reduzir o tamanho assim que for possível, mas, em vez disso, reduzir apenas se o número de entradas de índice se tornar pequeno em comparação com o tamanho da tabela de endereços de *bucket*.)

## Resposta
