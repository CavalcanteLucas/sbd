# 15-07

Escreva o pseudocódigo para um iterador que implementa junção por *loop* aninhado indexado, em que a relação externa é canalizada. Seu pseudocódigo precisa definir as funções padrão do iterador $open()$, $next()$ e $close()$. Mostre que informação de estado o iterador precisa manter entre as chamadas.

## Resposta
