# 15-12

A atual geração de CPUs inclui um *cache de instruções*, que mantém em cache as instruções usadas recentemnete. Então, uma chamada de função gera uma sobrecarga significativa, pois o conjunto de instruçlões sendo executadas muda, resultando em ausência no cache de instruções.

**a)** Explique por que a canalização controlada por produtor com uso de *buffer* provavelmente resultará em uma melhor taxa de presença (*hit rate*) no cache de instruções, em comparação com a canalização controlada por demanda.

**b)** Explique por que a modificação da canalização controlada por demanda, gerando múltiplos resultados em uma chamada a `next()` e retornando-os juntos, pode melhorar a taxa de presença no cache de instruções.

## Resposta
