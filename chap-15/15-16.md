# 15-16

Consider as colunas a seguir. Para cada uma, indique se o armazenamento orientado à coluna provavelmente seria benéfico ou não, explicando o motivo.

**a)** Buscar $ID$, $nome$ e $nome_dept$ do aluno com $ID$ `12345`.

**b)** Agrupar a relação $realiza$ por $ano$ e $id\_curso$, encontrando o número total de alunos para cada combinação ($ano$, $id\_curso$).

## Resposta
