# 18-16

Considere as funções $insert\_latchfree()$ e $delete\_latchfree()$, mostradas na Figura 18.23.

$$
\begin{aligned}
&insert\_latchfree(início,\ valor)\ \{\\
&\hspace{10pt} node = \textbf{new}\ nó\\
&\hspace{10pt} nó \rightarrow valor = valor\\
&\hspace{10pt}\textbf{repeat} \\
&\hspace{20pt} início\_antigo = início \\
&\hspace{20pt} nó \rightarrow seguinte = início\_antigo \\
&\hspace{20pt} resultado = CAS(início,\ início\_antigo,\ nó) \\
&\hspace{10pt}\textbf{until}(resultdo == \text{sucesso}) \\
&\} \\

&delete\_latchfree(início)\ \{\\
&\hspace{10pt}\textbf{repeat} \\
&\hspace{20pt} início\_antigo = início \\
&\hspace{20pt} início\_novo = início\_antigo \rightarrow seguinte\\
&\hspace{20pt} resultado = CAS(início,\ início\_antigo,\ início\_novo) \\
&\hspace{10pt}\textbf{until}(resultdo == \text{sucesso}) \\
&\} \\

\end{aligned}
$$

**Figura 18.23** - Inserção e exclusão sem tranca em uma lista.

**a)** Explique como o problema ABA pode ocorrer se um nó excluído for reinserido.

**b)** Suponha que, adjacente ao $início$, armazenemos um contador $cnt$. Suponha também que DCAS(($início$,$cnt$),($início\_antigo$,$cnt\_antigo$),($início\_novo$,$cnt\_novo$)) execute atomicamente um *compare-and-swap* sobre o valor de 128 bits ($início$, $cnt$). Modifique $insert\_latchfree()$ e $delete\_latchfree()$ para usar a operação DCAS de modo a evitar o problema ABA. 

**c)** Como a maioria dos processadores usa apenas 48 bits de um endereço de 64 bits para realmente endereçar a memória, explique como os outros 16 bits podem ser usados para implementar um contador, caso a operação DCAS não seja aceita.

## Resposta
