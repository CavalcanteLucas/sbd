# 18-07

Considere um sistema de banco de dados que inclui uma operação **increment** atômica, além das operações `read` e `write`. Considere que $V$ seja o valor do item de dados $X$. A operação 

$\textbf{increment}(X)\ \textbf{by}\ C$

define o valor de $X$ como $V + C$ em uma etapa atômica. O valor de $X$ não está disponível à transação, a menos que esta execute um `read(X)`.

Suponha que as operações de incremento bloqueiem o item no modo de incremento usando a matriz de compatibilidade da Figura 18.25.

**a)** Mostre que, se todas as transações bloquearem os dados que elas acessam no modo correspondente, o bloqueio em duas fases garante a serializabilidade.

**b)** Mostre que a inclusão dos bloqueios no modo de **incremento** permite maior concorrência.

## Resposta
