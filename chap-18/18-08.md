# 18-08

Na ordenação por *timestamp*, **W-*timestamp***$(Q)$ indica o maior *timestamp* de qualquer transação que executou `write(Q)` com sucesso. Suponha que, em vez disso, nós a definamos como o *timestamp* da transação mais recente a executar `write(Q)` com sucesso. Essa mudança de termos faz alguma diferença? Explique sua resposta.

## Resposta
