# 18-02

Cconsidere as duas transações a seguir:

$$
\begin{aligned}
T_{34}:&\ \text{read(A)}; \\
&\ \text{read(B)}; \\
&\ \text{\textbf{if} A = 0}\ \textbf{then}\ B := B + 1; \\
&\ \text{write(B)}; \\
\end{aligned}
$$

$$
\begin{aligned}
T_{35}:&\ \text{read(B)}; \\
&\ \text{read(A)}; \\
&\ \text{\textbf{if} B = 0}\ \textbf{then}\ A := A + 1; \\
&\ \text{write(A)}; \\
\end{aligned}
$$

Acrescente instruções de bloqueio e desbloqueio às transações $T_{31}$ e $T_{32}$, de modo que observem o protocolo de bloqueio em duas fases. A execução dessas transações poderá resultar em um empasse?

## Resposta
