# 09-07

O comando `netstat` (disponível no Linux e no Windows) mostra as conexões de rede ativas em um computador. Explique como esse comando pode ser usado para descobrir se uma página Web em particular não está fechado conexões que ela abriu ou se o *pooling* de conexões está sendo usado, não retornado conexões ao *pool* de conexões. Você deverá considerar o fato de que, com o *pooling* de conexões, a conexão pode não ser fechada imediatamente.

## Resposta
