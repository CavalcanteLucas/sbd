# 22-03

Com o paralelismo canalizado, normalmente é uma boa ideia realizar várias operações em um *pipeline* em um único processador, mesmo quando muitos processadores estão disponíveis.

**a)** Explique por quê.

**b)** Os argumentos que você usou no item (a) seriam mantidos se a máquina tivesse uma arquitetura de memória compartilhada? Explique por quê.

**c)** Os argumentos no item (a) seriam mantidos com o paralelismo independente? (Ou seja, existem casos em qque, mesmo que as operações não sejam canalizadas e há muitos nós à disposição, ainda é uma boa ideia realizar várias operações no mesmo nó?)

## Resposta
