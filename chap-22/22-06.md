# 22-06

Suponha que a relação $r$ seja mantida particionada e indexada sobre $A$, e $s$ seja particionada e indexada sobre $B$. Considere a junção $r\bowtie_{r\cdot B=s\cdot B}s$. Suponha que $s$ seja relativamente pequena, mas n'ao o suficiente para tomar a junção fragmentar e replicar assimétrica (*assymetric fragment-and-replace join*) a melhor opção, e $r$ seja grande, com a maioria das tuplas de $r$ não correspondendo a qualquer tupla de $s$. Uma junção *hash* pode ser realizada, mas com um filtro de semijunção sendo usado para reduzir a transferência de dados. Explique como a filtragem de semijunção usando filtros de Bloom funcionaria nessa configuração de junção paralela.

## Resposta
