# 22-05

Suponha que a relação $r$ seja armazenada particionada e indexada sobre $A$, e que $s$ seja armazenada particionada e indexada sobre $B$. Considere a consulta:

$$
r\cdot C \gamma_{count(s\cdot D)}((\sigma_{A>5}(r))\bowtie_{r\cdot B = s \cdot B} s)
$$

**a)** Indique um plano de consulta paralela usando o operador de troca para calcular a subárvore da consulta envolvendo apenas os operadores de seleção e junção.

**b)** Agora estenda isso para calcular a agregação. Lembre-se de usar a pré-agregação para reduzir a transferência de dados.

**c)** A distorção durante a agregação é um problema sério. Explique como a pré-agregação, conforme indicado anteriomente, pode reduzir significativamente o efeito da distorção durante a agregação.

## Resposta
