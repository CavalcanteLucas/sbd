# 08-04

Considere um esquema de banco de dados com uma relação $Emp$ cujos atributos sejam os seguintes, com os tipos especificados para os atributos de valores múltiplos:

<pre>
    Emp = (nome_emp, CjFilhos <b>multiset</b>(Filhos),
            CjHabilidades <b>multiset</b>(Habilidades))
    Filhos = (nome, nascimento)
    Habilidades = (tipo, CjExames <b>setof</b>(Exames))
    Exames = (ano, cidade)
</pre>

Defina esse esquema em SQL, usando a sintaxe do tipo **table** do SQL Server, da Seção 8.2.1.1, para declarar atributos de valores múltiplos.

## Resposta
