# 05-01

Considere as seguintes relações para um banco de dados de empresa:

- `emp(nome_e, nome_d, salário)`
- `gerente(nome_e, nome_g)`

e o código Java na Figure 5.20, que usa a API JDBC. Considere que userid, senha, nome de máquina etc. estejam todos corretos. Descreva, em português abreviado, o que o programa Java faz (ou seja, produza uma setntença em português como "Ele encontra o gerente do departamento de brinquedos", e não uma descrição linha por linha do que cada comando Java pode fazer).

```
import java.sql.*;
public class Mystery {
    public static void main(String [ ] args) {
        try (
            Connection con=DriverManager.getConnection(
                "jdbc:oracle:thin:star/X@//edgar.cse.lehigh.edu:1521/XE");
            q = "select nome_g from gerente where nome_e = ?";
            PreparedStatement stmt=con.prepareStatement();
        )
        {
            String q;
            String nome_e = "dog";
            boolean more;
            ResultSet result;
            do {
                stmt.setString(1, nome_e);
                result = stmt.executeQuery(q);
                more = result.next();
                if (more) {
                    empName = result.getString("nome_g");
                    System.out.println(nome_e);
                }
            } while (more);
            s.close();
            con.close();
        }
        catch(Exception e){
            e.printStackTrace();
        }
    }
}
```

## Resposta
